# Floors (>=) are used here so this reference solution keeps passing as the CVE
# feed moves - pip resolves to whatever is current at build time. That is a
# maintenance choice for an evergreen exercise, NOT the practice being taught:
# finish.md teaches exact pins plus an automated updater, because an
# unconstrained floor ships whatever pip resolved on build day with no
# reproducibility and no review. See scenarios/poisoned-present/readme.md.
#
# verify.sh scans the built image, not this file, so a student who writes
# urllib3==2.2.2 passes exactly like one who writes urllib3>=2.2.2.
fastapi>=0.122.0
uvicorn>=0.32.1
python-dotenv>=1.0.1
urllib3>=2.2.2
Pillow>=10.3.0
PyYAML>=6.0.1
requests>=2.32.0

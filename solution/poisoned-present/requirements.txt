# Every pin here is a floor (>=), never a ceiling. A floor lets pip resolve to
# whatever is current at build time, which is what keeps this solution passing as
# the CVE feed moves. An upper cap is what would freeze a package - and, through
# it, that package's own dependencies - below its patched release.
fastapi>=0.122.0
uvicorn>=0.32.1
python-dotenv>=1.0.1
urllib3>=2.2.2
Pillow>=10.3.0
PyYAML>=6.0.1
requests>=2.32.0

# Your Mission

Secure the Master Guidance Coordinates by updating the Helm chart at `~/masterclass-fastapi-app/`.

The current deployment exposes sensitive credentials in a way that the rogue elves can intercept. Your job is to reconfigure the deployment so that the application reads its credentials securely.

Investigate the Helm chart templates, the cluster resources, and the application's expectations. Then redeploy.

> 💡 If you're stuck, purchase hints from the challenge portal for guidance.

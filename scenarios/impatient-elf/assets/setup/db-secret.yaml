apiVersion: v1
kind: Secret
metadata:
  name: workshop-db
  namespace: workshop
  labels:
    app: workshop-db
type: Opaque
# Keys `user` / `password` / `database` are the ones the Pods read via secretKeyRef.
stringData:
  user: elf_ops
  password: h0-h0-h0-Sleigh-2026
  database: giftregistry

namespace: challenge1
image:
  repository: touching/masterclass-fastapi-app
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  port: 8000

env:
  mode: "production"
  # appToken is intentionally left empty: this is the insecure starting state
  # that participants must replace with a proper Kubernetes Secret volume mount.
  appToken: ""

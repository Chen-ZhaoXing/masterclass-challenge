### Help needed!

I have an application that uses a secret file at `/app/token` to store secrets.
I am using the values at `~/masterclass-fastapi-app/values.yaml` to deploy the application. The application is currently deployed at `challenge1` namespace.
Can you help me with that?

**Note**:It doesn't matter what the secret is, only that it is stored in a secret file.

apiVersion: v1
kind: Service
metadata:
  name: {{ include "masterclass-fastapi-app.fullname" . }}
  labels:
    {{- include "masterclass-fastapi-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "masterclass-fastapi-app.selectorLabels" . | nindent 4 }}

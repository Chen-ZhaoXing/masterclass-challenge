apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ctfd-intropage.fullname" . }}
  labels:
    {{- include "ctfd-intropage.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "ctfd-intropage.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "ctfd-intropage.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "ctfd-intropage.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

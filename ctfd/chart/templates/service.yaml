apiVersion: v1
kind: Service
metadata:
  name: {{ include "ctfd-intropage.fullname" . }}
  labels:
    {{- include "ctfd-intropage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ctfd-intropage.selectorLabels" . | nindent 4 }}
